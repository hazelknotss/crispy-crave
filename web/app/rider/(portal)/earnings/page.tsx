import { RiderStubShell } from "@/components/rider/rider-stub-shell";

export default async function RiderEarningsPage() {
  return (
    <RiderStubShell title="Earnings">
      <p className="text-muted small mb-0">
        Earnings summaries from the PHP rider portal will be ported here next (same data model as
        orders once synced).
      </p>
    </RiderStubShell>
  );
}
